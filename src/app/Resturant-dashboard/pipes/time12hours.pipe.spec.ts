import { Time12hoursPipe } from './time12hours.pipe';

describe('Time12hoursPipe', () => {
  it('create an instance', () => {
    const pipe = new Time12hoursPipe();
    expect(pipe).toBeTruthy();
  });
});
